import React from "react";

const Start = () => {
  return <div className="startContainer">Start</div>;
  // return <button onClick={() => alert("btn clicked")}>Start</button>;
};

export default Start;
